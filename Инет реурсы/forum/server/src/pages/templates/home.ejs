<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Форум</title>
</head>
<body>
    <a href="/page/categories">Категории</a>
    <a href="/page/login">Войти</a>
    <a href="/page/register">Зарегистрация</a>

    <% if (typeof user !== 'undefined' && user) { %>
        <div>
            <span>Привет, <%= user.username %>!</span>
            <a href="/logout" style="margin-left: 10px; color: #007bff;">Выйти</a>
        </div>
    <% } %>
</body>
</html>